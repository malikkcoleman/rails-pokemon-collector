<div id="<%= dom_id series %>">
  <p>
    <strong>Name:</strong>
    <%= series.name %>
  </p>

  <p>
    <strong>Image:</strong>
    <img src="<%= series.image %>" >
    <%= series.image %>
  </p>

</div>
